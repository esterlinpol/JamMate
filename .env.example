# Copy to .env and fill in. .env is gitignored and dockerignored — never commit it.
#
# Shared secret for the /api/sync/* endpoints. Must be IDENTICAL on every instance
# that syncs with this one. Empty or unset means sync is disabled (503), which is
# the safe default.
JAMMATE_SYNC_TOKEN=
