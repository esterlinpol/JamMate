services:
  jammate:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - jammate-data:/app/data
    networks:
      - proxy
    restart: unless-stopped

volumes:
  jammate-data:

networks:
  proxy:
    external: true
