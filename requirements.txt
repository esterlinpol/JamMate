# Server
fastapi>=0.115.0
jinjax>=0.45.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
aiofiles>=23.0.0
certifi>=2024.0.0

# Worker
demucs>=4.0.0
# Keep current: YouTube breaks yt-dlp every few months (HTTP 403 on the data
# fetch is the usual symptom). Upgrade first when a download starts failing.
yt-dlp>=2026.8.19
requests>=2.31.0
numpy>=1.24.0
soundfile>=0.12.0
# Forced lyric alignment (lyric_align.py). Pre-warm the model once so the first
# job doesn't stall for minutes on a silent download:
#   ./venv/bin/python -c "import stable_whisper; stable_whisper.load_model('small')"
stable-ts>=2.19.0
